#include "keyboard_generic.h"

void steno_scan(keyboard_type_t type, keyboard_definision_t definision);
void steno_init(keyboard_type_t type, keyboard_definision_t definision);
ret_code_t steno_sleep_prepare(keyboard_type_t type, keyboard_definision_t definision);