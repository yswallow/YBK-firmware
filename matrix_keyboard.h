#include "keyboard_generic.h"

void matrix_scan(keyboard_type_t type, keyboard_definision_t definision);
void matrix_init(keyboard_type_t type, keyboard_definision_t definision);
ret_code_t matrix_sleep_prepare(keyboard_type_t type, keyboard_definision_t definision);